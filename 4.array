// SPDX-License-Identifier: GPL-3.0

pragma solidity >=0.7.0 <0.9.0;

contract array 
{ 
    uint[] public app;

    function getpush(uint num) public 
    {
        app.push(num);
    }
    function getlength() view public returns(uint)
    {
        return app.length;
    }
     function getpop(uint) public 
    {
        app.pop();
    }



}

